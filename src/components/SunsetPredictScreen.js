import React from 'react';
import { StyleSheet, Text, View } from 'react-native';

const SunsetPredictScreen = () => {
    return (
        <Text>hey</Text>
    );
}

export default SunsetPredictScreen;